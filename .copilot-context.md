# Copilot Context - Aurora Studio

## 🎯 Purpose

This file tells AI assistants the exact state of your project. Use this when asking for new features or fixes to avoid unnecessary rebuilds and credit waste.

---

## 📍 Current Project State

### Latest Stable Version
- **Version:** 1.1.0 (CLI Release)
- **Branch:** main
- **Date:** 2026-06-13
- **Status:** Production-ready, CLI published to npm as `aurora-studio` v0.3.1

### What's Already Built

#### Features
- ✅ Motion Studio (`/motion` route) — pose-based video generation
- ✅ Lipsync Gallery (`/lipsync` route) — avatar lipsync rendering
- ✅ Provider Orchestration — Gemini → HF → Replicate → Lovable → GPU → Fal fallback chain
- ✅ HeyGen Avatars — avatar-based video generation
- ✅ Kling Direct Video — JWT-signed video generation
- ✅ User Webhooks — event-based callbacks with HMAC signing
- ✅ Affiliate Program — referral tracking and payouts
- ✅ Gift Cards — credit redemption system
- ✅ Paystack Integration — payment processing and webhooks
- ✅ Modular Email Templates — 6 transactional emails
- ✅ Provider Status API — check configured providers
- ✅ GPU Worker Health — worker monitoring and status
- ✅ Color Export — palette export (JSON, CSS, Tailwind)
- ✅ Aurora CLI — `npm i -g aurora-studio` (published to npm)
- ✅ CLI Device Auth — `/cli/authorize` route + `/api/public/cli/device/*` endpoints
- ✅ Public Generate API — `/api/public/generate` for CLI-driven generations

#### Components (Existing)
```
src/components/
├── ConnectReplicateBanner.tsx      # Replicate connector prompt
├── HeyGenPanel.tsx                 # Avatar selection UI
├── landing/DemoReels.tsx           # Video demo showcase
├── studio/ColorsShotsGallery.tsx   # Color gallery with animations
└── [other existing components]
```

#### Libraries (Existing)
```
src/lib/
├── orchestrator.server.ts          # Provider routing logic
├── orchestration.functions.ts      # Health tracking
├── heygen.server.ts                # HeyGen API integration
├── webhooks.server.ts              # Webhook management
├── affiliate-complete.functions.ts # Affiliate system
├── gifts-complete.functions.ts     # Gift card system
├── paystack-webhook.server.ts      # Payment webhooks
├── emails.server.ts                # Email templates
├── lipsync-gallery.functions.ts    # Lipsync history
├── ugc-generation.functions.ts     # Product ad generation
├── provider-status.functions.ts    # Provider availability
├── gpu-worker-health.ts            # Worker monitoring
├── error-toasts.ts                 # Error messaging
├── og-image.server.ts              # Social sharing
├── colors-export.ts                # Palette export
└── [other existing libraries]
```

#### Routes (Existing)
```
/                  # Landing page
/studio            # Main generation studio
/motion            # Motion studio
/lipsync           # Lipsync gallery
/dashboard         # User dashboard
/dashboard/webhooks # Webhook management
/dashboard/gifts    # Gift card interface
/dashboard/affiliates # Affiliate tracking
```

#### Database Schema (Current)
```sql
-- Tables
generations        # With new 'input_videos' JSON field
user_webhooks      # Webhook registration & logs
gift_cards         # Gift card tracking
affiliates         # Affiliate program users
affiliate_events   # Click & conversion tracking
profiles           # User profiles
users              # Auth users
```

#### Environment Variables (Configured)
```
REQUIRED:
  LOVABLE_API_KEY
  SUPABASE_URL
  SUPABASE_ANON_KEY

OPTIONAL (Orchestration Providers):
  GEMINI_API_KEY          # Free image generation
  REPLICATE_API_KEY       # Video/image generation
  HEYGEN_API_KEY          # Avatar lipsync
  KLING_ACCESS_KEY        # Video generation
  KLING_SECRET_KEY
  FAL_KEY                 # Catch-all fallback
  OPENROUTER_API_KEY      # Text inference
  HF_TOKEN                # HuggingFace inference

OPTIONAL (Monetization):
  PAYSTACK_SECRET_KEY     # Payment processing
```

---

## 🚫 DO NOT Rebuild These

**If you see a request to "rebuild the UI" or "redesign the dashboard" without specific changes:**

1. ✋ STOP — Components already exist
2. Check: `src/components/` directory
3. Check: `src/routes/` directory
4. Check: `src/config/ui-state.ts` for current state
5. Reference: `UI_SNAPSHOT.md` for visual documentation

Instead, ask for **specific modifications:**
- "Change the sidebar width from 250px to 300px"
- "Update the header background color to #..."
- "Add a new button to the Motion studio page"

---

## 📖 How to Use This File

### When Starting a New Task

**Before:** "Please rebuild my dashboard because I imported it into a new workspace"

**After:** Reference this file to the AI:
> "I'm continuing work on Aurora Studio v1.0 (see .copilot-context.md for current state). I want to [specific change]. Don't rebuild—just modify [specific file]."

### When Asking for New Features

**Example:**
> "See .copilot-context.md for what exists. I want to add [new feature]. Use existing [component/library] and integrate with [existing system]."

### When Debugging

**Example:**
> "The Motion studio is broken (see .copilot-context.md for architecture). Check src/components/landing/DemoReels.tsx and src/lib/heygen.server.ts for issues."

---

## 🔗 Critical Files (Reference, Don't Modify)

### Documentation
- `FEATURE_SUMMARY.md` — What v1.0 includes
- `PR_DESCRIPTION.md` — Technical details
- `TESTING_GUIDE.md` — How to test features
- `CHANGELOG.md` — Release notes
- `UI_SNAPSHOT.md` — Visual reference
- `QUICK_START.md` — Setup instructions
- `src/config/ui-state.ts` — Default UI state

### Core Infrastructure
- `src/lib/orchestrator.server.ts` — Provider routing (CRITICAL)
- `src/integrations/supabase/types.ts` — Database schema
- `.env.example` — Required environment variables

---

## 💡 Credit-Saving Tips

### ❌ Costs Credits (Don't Do This)
```
"My app looks broken, please fix it"
→ AI rebuilds entire UI from scratch → 5-10 credits

"The motion studio doesn't work"
→ AI re-implements motion logic → 5+ credits

"Import my repo and set it up"
→ AI configures everything again → 3-5 credits
```

### ✅ Saves Credits (Do This)
```
"The motion studio button color is wrong. Change it from blue to red in src/components/landing/DemoReels.tsx"
→ AI makes 1-line change → 0.5 credits

"Add a new provider (Midjourney) to the orchestration chain. Follow the Kling pattern in orchestrator.server.ts"
→ AI copies pattern and adapts → 1-2 credits

"The webhook isn't firing. Check src/lib/webhooks.server.ts and verify the dispatch logic"
→ AI debugs specific file → 1-2 credits
```

---

## 🎯 When to Reference This File

**Always include this context when:**
- Asking for modifications to existing features
- Debugging issues
- Adding new features (reference existing patterns)
- Deploying or setting up in new environments
- Importing to new workspaces/accounts
- Asking "Is this already built?"

**Example Prompt:**
```
I'm working on Aurora Studio v1.0 (see .copilot-context.md).
I need to add Slack notifications to the webhook system.
Use the existing webhook pattern in src/lib/webhooks.server.ts.
Add a new "slack_notify" event type.
Don't rebuild—just extend what exists.
```

---

## 📞 Quick Reference

| Need | File | Cost |
|------|------|------|
| Setup instructions | QUICK_START.md | Free |
| What's built | FEATURE_SUMMARY.md | Free |
| Visual reference | UI_SNAPSHOT.md | Free |
| Architecture | PR_DESCRIPTION.md | Free |
| How to test | TESTING_GUIDE.md | Free |
| Modify header | src/components/Header.tsx | 0.5 credits |
| Fix provider | src/lib/orchestrator.server.ts | 1-2 credits |
| Add feature (using existing patterns) | Relevant library | 2-3 credits |
| Rebuild UI from scratch | Start over | 10+ credits |

---

**Last Updated:** 2026-06-11  
**Version:** 1.0.0  
**Status:** Current
