# Creating an App
Source: https://app.inference.sh/docs/extend/creating-app

Scaffold a new app with the CLI.

---

## Create App

```bash
infsh app init my-app

# Node.js app
infsh app init my-app --lang node
```

Or interactive mode:

```bash
infsh app init
```

---

## What's Created

<div data-type="codegroup">

```text Python
my-app/
├── inf.yml           # Configuration
├── inference.py      # Your code
├── requirements.txt  # Python dependencies
├── packages.txt      # System dependencies (optional)
├── skills/           # AI coding agent guidance
└── README.md
```

```text Node.js
my-app/
├── inf.yml           # Configuration
├── src/
│   └── inference.js  # Your code
├── package.json      # Node.js dependencies
├── packages.txt      # System dependencies (optional)
├── skills/           # AI coding agent guidance
└── README.md
```

</div>

---

## Files Explained

| File | Python | Node.js | Purpose |
|------|--------|---------|---------|
| `inf.yml` | Yes | Yes | App settings, resources |
| `inference.py` | Yes | — | App logic (Python) |
| `src/inference.js` | — | Yes | App logic (Node.js) |
| `requirements.txt` | Yes | — | Python packages (pip) |
| `package.json` | — | Yes | Node.js packages (npm/pnpm) |
| `packages.txt` | Yes | Yes | System packages (apt) — optional |
| `skills/` | Yes | Yes | Guidance for AI coding assistants |

---

## Dependencies

### App Packages

<div data-type="codegroup">

```text requirements.txt (Python)
torch>=2.0
transformers
accelerate
```

```json package.json (Node.js)
{
  "type": "module",
  "dependencies": {
    "zod": "^3.23.0"
  }
}
```

</div>

### System Packages (packages.txt)

For system-level dependencies (installed via apt):

```
ffmpeg
libgl1-mesa-glx
```

---

## Base Images

Apps run in containers with these base images:

| Type | Image | Use Case |
|------|-------|----------|
| GPU | `docker.inference.sh/gpu:latest-cuda` | CUDA GPU apps |
| CPU | `docker.inference.sh/cpu:latest` | CPU-only apps |

> **Note:** Currently only NVIDIA CUDA GPUs are supported for GPU apps.

---

## Next

→ [App Code](./app-code)
